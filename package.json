{
  "name": "codeden",
  "version": "1.0.0",
  "description": "A beginner's platform to learn coding for free",
  "main": "server/app.js",
  "scripts": {
    "start": "node server/app.js",
    "build": "cd client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Robert Doe",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.17.3"
  }
}
